# Normalize line endings to LF in the repository.
* text=auto eol=lf

# Scripts that must keep their platform line endings
*.bat   text eol=crlf
*.ps1   text eol=crlf

# Binary assets
*.png   binary
*.jpg   binary
*.jar   binary
*.so    binary
*.dll   binary
*.keystore binary
*.jks   binary
